require(['Lity'], function(lity) {

    // class LityClass {

    //     constructor(params) {

    //         $(document).on('click', params.selector, function(){
				//     	let lightbox = lity(params.target);
				//     });

    //     }

    // }

    // const lightboxes = $('[data-lightbox]')

    // $.each(lightboxes, function(i, e) {
    //     let section = new SectionClass(e, i)
    //     section.pathPrepare();
    //     section.createScene();
    // })

    // let cane = new LityClass({selector:'.button--arctic',target:'http://www.youtube.com/watch?v=XSGBVzeBUbk'})

    

});